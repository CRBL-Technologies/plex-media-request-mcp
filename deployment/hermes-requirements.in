# Search-only dependency for Hermes' bundled DuckDuckGo provider.
ddgs==9.14.4

# Preserve the pinned Hermes base environment instead of opportunistically
# upgrading transitive packages while adding search. AnyIO includes the
# security fixes in 4.14.2 rather than retaining the base image's version.
anyio==4.14.2
certifi==2026.5.20
idna==3.18
typing-extensions==4.15.0
